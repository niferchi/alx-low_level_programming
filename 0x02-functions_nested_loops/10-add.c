#include "main.h"

/**
 * add - is a function that adds two ints and return its result
 * Return: the added value of two ints
 * @first: first int
 * @second: second int
 */

int add(int first, int second)
{
	return (first + second);
}
